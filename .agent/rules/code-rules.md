---
trigger: always_on
---

# Unified Claude Instructions

このリポジトリの唯一の規約ファイルは以下です。

- `code-rules/AGENTS.md`（詳細参照版）

適用ルール:

1. Claude CLI は必ず `code-rules/AGENTS.md` を読み込み、本ファイルはリダイレクトとして扱う。
2. 本ファイルに個別規約を追記しない。
3. 競合時は `code-rules/AGENTS.md` を優先する。
