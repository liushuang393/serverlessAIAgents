# -*- coding: utf-8 -*-
# =============================================================================
# AgentFlow 環境変数設定テンプレート
# =============================================================================
# このファイルを .env にコピーして、実際の値を設定してください。
# cp .env.example .env
#
# 松耦合設計：環境変数から自動検出、プロバイダー指定不要
# =============================================================================

# -----------------------------------------------------------------------------
# LLM プロバイダー（優先順位: OpenAI > Anthropic > Google > Azure > Ollama）
# -----------------------------------------------------------------------------
# OpenAI
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o-mini

# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-xxx
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Google Gemini
GOOGLE_API_KEY=AIza-xxx
GOOGLE_MODEL=gemini-2.0-flash

# Azure OpenAI
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=https://xxx.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# Ollama（ローカル）
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# LocalAI（ローカル）
LOCALAI_BASE_URL=http://localhost:8080
LOCALAI_MODEL=gpt-4

# -----------------------------------------------------------------------------
# データベース（優先順位: Supabase > PostgreSQL > Turso > SQLite）
# -----------------------------------------------------------------------------
# Supabase（推奨）
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=eyJxxx
SUPABASE_SERVICE_KEY=eyJxxx

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Turso（SQLite エッジ）
TURSO_URL=libsql://xxx.turso.io
TURSO_AUTH_TOKEN=xxx

# SQLite（ローカル）
SQLITE_PATH=./data/local.db

# -----------------------------------------------------------------------------
# ベクトルデータベース（優先順位: Pinecone > Qdrant > ChromaDB）
# -----------------------------------------------------------------------------
# Pinecone
PINECONE_API_KEY=xxx
PINECONE_INDEX=agentflow-index
PINECONE_ENVIRONMENT=us-east-1

# Qdrant
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=agentflow

# ChromaDB（ローカル）
CHROMA_PERSIST_DIR=./data/chroma

# -----------------------------------------------------------------------------
# Embedding（優先順位: OpenAI > SentenceTransformer）
# -----------------------------------------------------------------------------
# OpenAI Embedding（OPENAI_API_KEY 共用）
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ローカル Embedding（SentenceTransformer）
USE_LOCAL_EMBEDDING=false
LOCAL_EMBEDDING_MODEL=all-MiniLM-L6-v2

# -----------------------------------------------------------------------------
# アプリケーション設定
# -----------------------------------------------------------------------------
# ログレベル
LOG_LEVEL=INFO

# サーバー設定
HOST=0.0.0.0
PORT=8000

# CORS（カンマ区切り）
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# -----------------------------------------------------------------------------
# オプション：外部サービス
# -----------------------------------------------------------------------------
# Tavily Search API
TAVILY_API_KEY=tvly-xxx

# SerpAPI
SERPAPI_API_KEY=xxx

# GitHub（MCP用）
GITHUB_TOKEN=ghp_xxx

