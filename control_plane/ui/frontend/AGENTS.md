# Unified Agent Instructions

このリポジトリの規約ソースは **1つだけ** です。

- Canonical Rules: `code-rules/AGENTS.md`（詳細参照版）

適用ルール:

1. Codex / Claude CLI / Cursor / Augment を含む全エージェントは、必ず `code-rules/AGENTS.md` を読み込んで従う。
2. このファイル（AGENTS.md）はローカル規約を持たない。差分規約を追加しない。
3. 競合時は常に `code-rules/AGENTS.md` を優先する。
