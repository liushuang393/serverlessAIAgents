name: agentflow
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - pip
  - pip:
      - pydantic>=2.0.0
      - click>=8.0.0
      - rich>=13.0.0
      - httpx>=0.27.0
      - pyyaml>=6.0.0
      - jinja2>=3.1.0
      - fastapi>=0.115.0
      - uvicorn[standard]>=0.32.0
      - websockets>=13.0
      - pytest>=8.0.0
      - pytest-cov>=6.0.0
      - pytest-asyncio>=0.24.0
      - mypy>=1.13.0
      - ruff>=0.8.0
      - types-pyyaml>=6.0.0
      - types-requests>=2.32.0
